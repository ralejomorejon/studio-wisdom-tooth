name = "wisdom-tooth-sanity-webhook"
main = "worker.ts"
compatibility_date = "2026-03-30"
compatibility_flags = ["nodejs_compat"]

[build]
command = "npm install && tsc worker.ts --outDir dist --module esnext --target es2020 --lib es2020 --types @cloudflare/workers-types --skipLibCheck"
cwd = "."
